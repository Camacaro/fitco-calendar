
export class Event {
  constructor(uuid, title, notes, startDate, endDate) {
    this.uuid = uuid;
    this.title = title;
    this.notes = notes;
    this.startDate = startDate;
    this.endDate = endDate;
  }
}
